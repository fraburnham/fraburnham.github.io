(ns devils-grip.ui.engine)

;; make the ui data map like
;;{:ui {:board {:row# {:col# {:details} :col# {:details}} :row# {:etc}} :stock {} :talon {}}
;; then only need a couple fns. one to walk & set all details back to default

;; start w/ only the details for transparency

;; think all this through a bit further.
